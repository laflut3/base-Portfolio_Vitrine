"use client";

import SectionAdminProjects from "@/components/utils/project/SectionAdminProjects";

export default function AdminProjectPage() {
    return (
        <main className="flex items-center justify-center min-h-screen">
            <SectionAdminProjects/>
        </main>
    )
}
